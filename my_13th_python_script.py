#! usr/bin/env python3
#range can be specified as below and printed as a list
range(100)
print(list(range(1,100)))

#range can be put in for loop but in this sutation it will be printed as idividual number on the line
for num in range(100):
		if num>1:
			print(num)
	
